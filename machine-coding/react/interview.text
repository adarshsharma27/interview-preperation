Write a React functional component that renders a responsive navigation bar using Tailwind CSS. The navbar should collapse into a hamburger menu on small screens and expand on larger screens. Implement the toggle logic using React hooks.
Given a React functional component, write a custom hook usePrevious that returns the previous value of a prop or state variable. Show how you'd use it within a component to display both current and previous values.
You mentioned using try-catch blocks for error handling and showcasing errors via toast messages. Can you clarify how you would design a retry mechanism for failed API requests? Specifically, how would you manage retry intervals and limit the number of retries to avoid overloading the server?
Let's move to a coding challenge. Write a React function component that fetches a list of users from a given REST API endpoint (e.g., 'https://jsonplaceholder.typicode.com/users'). Display the list of user names on the page. Ensure you handle loading states, error states, and implement a retry mechanism that retries the API request up to 3 times with a 2-second delay between retries if it fails.